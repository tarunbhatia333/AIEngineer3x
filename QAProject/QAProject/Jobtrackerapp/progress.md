# Progress Log

## 2026-06-13 — Job Pathway React App
- Created a new React/Vite application in `JobPathway/` for the job tracker.
- Built a drag-and-drop board with the required stages: Job Saved, Applied, In Progress, Follow-up, Interview, Offer, Rejected.
- Added search, sort, theme switching, JSON export/import, card preview, and stage reminders.
- Added multi-theme support with persistent settings using `localStorage`.
- Implemented a top-stage carousel for status cards, plus a header +Add Job button and expanded add-job modal fields.
- Added mandatory job title and card dropdown selection in the popup, plus optional HR email, phone, company, resume, and description fields.
- Prepared the app for local preview on localhost.
